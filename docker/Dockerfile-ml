# Use the official Python 3.10 slim image as the base.
# The "slim" variant is a good choice as it includes the minimum packages
# needed to run Python, resulting in a smaller image size.
FROM python:3.10-slim

# Set the working directory inside the container to /app.
# This is where your commands will run and where files will be copied.
WORKDIR /app

# Update pip and install the specified Python libraries.
# We use --no-cache-dir to prevent pip from storing the downloaded packages,
# which helps to keep the final image size smaller.
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir pandas tensorflow numpy

# By default, when the container starts, it will launch an interactive bash shell.
# This allows you to immediately start working within the container's environment.
CMD ["bash"]
